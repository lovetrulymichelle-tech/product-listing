{
  "name": "product-listing",
  "version": "1.0.0",
  "description": "A simple product listing application",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node src/app.js",
    "test": "npm run test:unit",
    "test:unit": "node_modules/.bin/mocha test/*.test.js || echo 'Mocha not installed, skipping tests'",
    "lint": "echo 'No linting configured' && exit 0"
  },
  "keywords": [
    "product",
    "listing",
    "ecommerce"
  ],
  "author": "Michelle",
  "license": "MIT",
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
